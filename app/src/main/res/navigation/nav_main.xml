<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_main"
    app:startDestination="@id/mainFragment"
    tools:ignore="UnusedNavigation">
    <fragment
        android:id="@+id/mainFragment"
        android:name="com.koma.movie.MainFragment"
        android:label="@string/app_name">
        <action
            android:id="@+id/showCategoryMovie"
            app:destination="@id/movieCategoryFragment" />
    </fragment>

    <fragment
        android:id="@+id/movieCategoryFragment"
        android:name="com.koma.movie.home.MovieCategoryFragment"
        android:label="@string/app_name" />
</navigation>